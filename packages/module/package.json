{
  "name": "@astiamicii/monorepo-example",
  "description": "module is used to play with the npm publish process using lerna and travis ci.",
  "version": "1.12.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage && coveralls < coverage/lcov.info"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^25.5.4"
  }
}
